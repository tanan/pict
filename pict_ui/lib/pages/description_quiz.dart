import 'package:flutter/material.dart';

class DescriptionQuizPage extends StatelessWidget {
  const DescriptionQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("DescriptionQuizPage"),
    );
  }
}
